<!DOCTYPE html>
<html>
  <head>
    <title>KindergartenExchange</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@48,400,0,0" />
    <link href="https://fonts.googleapis.com/css2?family=Quicksand&family=Raleway:wght@100&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  </head>

  <body>
    <header class="selection:bg-blue-500 selection:text-white">
      <nav>
        <div class="flex flex-row justify-between items-center text-white bg-gray-600 px-[1rem] py-[1rem] sm:px-[1.5rem] sm:py-[1.5rem]">
          <%= link_to "Root", root_path %>
          <ul class="fixed top-0 right-0 hidden md:block md:relative md:flex-row md:justify-between" id="navbar">
            <div class="flex flex-col md:flex-row gap-4 md:gap-0 md:mt-0 px-[1rem] py-[1rem] sm:px-[1.5rem] sm:py-[1.5rem] md:p-0 min-h-[100vh] md:min-h-0 min-w-[40vw] md:min-w-0 bg-gray-600/[0.95] z-1 md:z-0 rounded-lg">
              <li class="text-left cursor-pointer md:hidden" id="navbar__closeButton">
                <span class="material-symbols-rounded hover:text-blue-400">
                  close
                </span>
              </li>
              <hr class="md:hidden">
              <li>
                <a class="p-2 cursor-pointer hover:bg-blue-400 hover:text-white md:p-3 rounded-md hover:underline" href="#">About</a>
              </li>
              <hr class="md:hidden">
              <li>
                <a class="p-2 cursor-pointer hover:bg-blue-400 hover:text-white md:p-3 rounded-md hover:underline" href="#">FAQ</a>
              </li>
              <hr class="md:hidden">
              <li>
                <a class="p-2 cursor-pointer hover:bg-blue-400 hover:text-white md:p-3 rounded-md hover:underline" href="#">Send a request</a>
              </li>
              <hr class="md:hidden">
              <li>
                <a class="p-2 cursor-pointer hover:bg-blue-400 hover:text-white md:p-3 rounded-md hover:underline" href="#">Contact</a>
              </li>
              <hr class="md:hidden">
              <li>
                <% if current_user.present? %>
                  <span data-test="current-user-email"><%= current_user.email %></span>
                  <%= link_to "Admin Dashboard", admin_dashboard_path if current_user.admin? %>                 
                  <%= button_to "Sign out", destroy_user_session_path, method: :delete, form_class: "" %>
                <% else %>
                  <%= link_to "Login", new_user_session_path %>
                <% end %>                              
              </li>
              <hr class="md:hidden">
            </div>
          </ul>
          <div class="cursor-pointer md:hidden" id="navbar__menuButton">
            <span class="material-symbols-rounded">
              menu
            </span>
          </div>
        </div>			
      </nav>
    </header>
    <p data-test="notice" class="notice"><%= notice %></p>
    <p data-test="alert" class="alert"><%= alert %></p>

    <%= yield %>

    <footer class="flex flex-col items-center text-black text-base font-medium text-center p-[5vw] selection:bg-blue-500 selection:text-white">
      <div class="flex flex-row justify-center mb-4 gap-4">
        <a href="#">
          <i class="fa fa-envelope hover:text-blue-500" style="font-size:3rem;"></i>
        </a>
        <a href="#">
          <i class="fa fa-facebook hover:text-blue-500" style="font-size:3rem;"></i>
        </a>
        <a href="#">
          <i class="fa fa-twitter hover:text-blue-500" style="font-size:3rem;"></i>
        </a>
        <a href="#">
          <i class="fa fa-github hover:text-blue-500" style="font-size:3rem;"></i>
        </a>
      </div>
      <p class="text-xs">
        Copyright © Kindergarten Exchange 2023
      </p>
    </footer>
  </body>
</html>
